{% extends 'base.html.twig' %}

{% block title %}City{% endblock %}

{% block body %}
<div class="container">
    <h1>{{ 'City'|trans }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ city.id }}</td>
            </tr>
            <tr>
                <th>{{ 'Name'|trans }}</th>
                <td>{{ city.name }}</td>
            </tr>
            <tr>
                <th>{{ 'ShippingCost'|trans }}</th>
                <td>{{ city.shippingCost }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_city_index') }}">back to list{{ 'Back to list'|trans }}</a>

    <a href="{{ path('app_city_edit', {'id': city.id}) }}">{{ 'edit'|trans }}</a>

    {{ include('city/_delete_form.html.twig') }}
</div>
{% endblock %}
