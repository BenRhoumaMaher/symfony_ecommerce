{% extends 'base.html.twig' %}

{% block title %}{{ 'Edit City'|trans }}{% endblock %}

{% block body %}
    <div class="container">
    <h1>{{ 'Edit City'|trans }}</h1>

    {{ include('city/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_city_index') }}">{{ 'back to list'|trans }}</a>

    {{ include('city/_delete_form.html.twig') }}
</div>
{% endblock %}
